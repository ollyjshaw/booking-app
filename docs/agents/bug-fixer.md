# Bug Fixer

## Trigger
<!-- What label fires this agent? Likely: `bug` -->

## Remit
<!-- Fix one bug. That is all. -->

## Process
<!-- Step by step what it does, in order.
     Should include: reproduce first (failing test), fix minimal
     code path, confirm test passes, run full suite, open PR. -->

## Output
<!-- A PR containing: the failing test + the fix. Nothing else. -->

## Hard Constraints
<!-- What is it absolutely forbidden from doing?
     This list should be long and specific.
     E.g. no refactoring, no renaming, no tidying adjacent code,
     if it spots another bug it opens a new issue and stops. -->
