# Agents

<!-- Explain the multi-agent setup here once it's established.
     Cover:
     - What agents exist and what each one does
     - The order agents run (e.g. Domain Architect before System Architect
       before Feature Implementer)
     - How a human fits into the loop
     - What triggers each agent (labels, schedule, PR events)
     - What "done" looks like for each agent -->
