# Aggregates

<!-- Define each aggregate root and its invariants.
     An invariant is a rule that must always be true.
     The aggregate is responsible for enforcing it.

     Format each entry as:

## AggregateName (Aggregate Root)
What it represents.

### Invariants
- Rule that must always hold
- Another rule

### State / Lifecycle
Optional: a state machine if the aggregate has meaningful states.

-->
