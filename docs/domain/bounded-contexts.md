# Bounded Contexts

<!-- Define the boundaries of the model.
     Where does one context end and another begin?
     How do they communicate (if at all)?

     For a small project this may be a single context to start.
     That's fine — document it anyway so the boundary is explicit
     and expansion is deliberate. -->
