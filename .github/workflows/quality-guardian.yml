name: Quality Guardian

# Runs on a schedule and on every PR (read-only, comment only)
# Adjust the cron schedule to taste

on:
  schedule:
    - cron: '0 2 * * *'   # nightly at 2am UTC — adjust as needed
  pull_request:
    types: [opened, synchronize]

jobs:
  quality-guardian:
    runs-on: ubuntu-latest
    steps:
      # Read docs/agents/quality-guardian.md for the full remit
      # and constraints before configuring this workflow.
      - name: Placeholder
        run: echo "Quality Guardian workflow — not yet configured"
